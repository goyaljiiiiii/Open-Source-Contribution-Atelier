# Create SSOC labels and issue automation

Add repository labels such as `SSOC`, `ssoc:good-first-issue`, `ssoc:mentor-needed`, and update issue templates to include these labels automatically.

Acceptance criteria:
- Create label suggestions in project settings (document them in `.github/labels.md`)
- Update issue templates to include `SSOC` label where appropriate

Labels: SSOC, infra
