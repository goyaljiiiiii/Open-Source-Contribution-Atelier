These are GitHub issue draft files consumed by `scripts/create_github_issues.py`.

Format per file:
- Line 1: Title (may start with #)
- Remaining lines: Body
- Optional: `Labels:` line with comma-separated labels

